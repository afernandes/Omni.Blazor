@namespace Omni.Blazor.Components

@*  Conveniência: monta de uma vez os 4 hosts de overlay da lib (Dialog,
    Notification, Tooltip, ContextMenu). Coloque uma vez no layout raiz
    (AppShell) — todos os serviços correspondentes encontram seus hosts.

    NÃO confundir com <OmniOverlay /> (singular) — esse é um componente
    standalone pra você fazer seu próprio overlay (backdrop + scroll lock +
    Esc handler). Este aqui é só o container plural dos hosts existentes. *@

<OmniDialogHost />
<OmniNotificationHost />
<OmniTooltipHost />
<OmniContextMenuHost />
<OmniTourHost />
