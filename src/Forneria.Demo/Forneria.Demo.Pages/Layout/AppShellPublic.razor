@inherits LayoutComponentBase

@*  Layout minimalista para páginas customer-facing standalone (cardápio
    digital, link de pagamento, página de confirmação, etc.) que devem
    ocupar a tela inteira sem nav/sidebar/footer corporativos. Apenas
    @Body + OmniOverlayHosts.

    Cada página renderizada aqui é responsável pelo próprio chrome
    (cover image, header, cart bar, footer próprio). *@
@Body

<OmniOverlayHosts />
