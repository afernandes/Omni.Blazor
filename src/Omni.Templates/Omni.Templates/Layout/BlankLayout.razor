@namespace Omni.Templates.Layout
@inherits LayoutComponentBase

@* Chrome-free layout: renders a template exactly as it would look inside a real
   app, with none of the gallery shell around it. Used by the "Tela cheia" links
   (TemplateDoc.FullScreenPath). *@

@Body

<OmniOverlayHosts />
