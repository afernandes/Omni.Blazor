@page "/templates/shell/full"
@layout BlankLayout

@* The app shell exactly as it looks in a real app — no gallery chrome.
   Linked from the gallery page via TemplateDoc.FullScreenPath. *@

<PageTitle>App shell (tela cheia) — Omni Templates</PageTitle>

<AppShell Root="/templates/shell/full" />
